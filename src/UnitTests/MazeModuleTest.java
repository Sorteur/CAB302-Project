package UnitTests;

public class MazeModuleTest {
    /*
    MazeModule test = new MazeModule(DBConnection.Instance());
        try {test.SaveMaze(new Maze(10,10,"Test"));}
        catch (SQLException sqle)
        {System.out.println(sqle);}
     */
}
